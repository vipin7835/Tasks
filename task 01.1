Check Whether a number entred by the user is even or odd by using linux shell scripting input will be user user prompt.
#!/bin/bash

# Input will be user prompt

echo "Enter a number"
read num
if [ $((num%2)) -eq 0 ]; then
        echo "$num is even."
else
        echo "$num is odd."
fi
